# 需求文档：审核策略服务模块拆分

## 背景

审核策略服务目前将配置管理、策略执行、统计回写混在一个模块中，导致变更成本高、职责边界不清。计划在不改变外部接口的前提下完成模块拆分。

## 目标

- 拆分配置管理、执行编排、统计回写职责
- 优先复用已有入口和数据结构
- 明确迁移顺序、故障场景和回退方案

## 约束

- 不允许先写最终方案再回填中间产物
- 需要针对职责转移建立现有资产候选比较
- 最终文档仍然落到 `.architecture/technical-solutions/`
